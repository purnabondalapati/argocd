apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
  # annotations:
  #   nginx.ingress.kubernetes.io/rewrite-target: /

spec:
  rules:
  - host: "ec2-65-0-203-239.ap-south-1.compute.amazonaws.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
           service:
             name: {{ include "nginx.fullname" . }}
             port: 
               number: 80
